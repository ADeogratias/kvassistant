/*
* Model class for message
* is use to store message and sender
* to display in chat screen
*
 */
class MessageModel {
  String? message;
  bool? isSender;

  MessageModel({this.message, this.isSender});
}
